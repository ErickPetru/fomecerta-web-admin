{
  "functions": {
    "source": "functions"
  },
  "hosting": {
    "predeploy": [
      "npm --prefix src run build && rm -rf functions/nuxt && cp -r src/nuxt/ functions/nuxt/ && cp src/nuxt.config.js functions/",
      "rm -rf public && mkdir public/_nuxt && cp -a functions/nuxt/dist/client/. public/_nuxt/ && cp -a src/static/. public/"
    ],
    "public": "public",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "function": "nuxtSSR"
      }
    ]
  }
}
